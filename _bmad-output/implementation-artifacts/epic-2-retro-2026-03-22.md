# Epic 2 회고 — Dashboard 홈: 리그 현황 한눈에 보기

**날짜:** 2026-03-22
**Epic:** 2 — Dashboard 홈
**완료율:** 4/4 스토리 (100%)

---

## 완료 스토리 요약

| 스토리 | 내용 | 코드 리뷰 패치 수 |
|--------|------|-----------------|
| 2.1 Dashboard 레이아웃 | shadcn CSS 변수 24개 매핑, Teal 사이드바 활성 스타일 | 1 |
| 2.2 EloLeaderboard | 리더보드 테이블 + Skeleton + 자신 행 하이라이트 | 3 |
| 2.3 ChampionTierTable | S/A/B/C 티어 그룹핑 + 접기/펼치기 + useMemo | 2 |
| 2.4 BanTrendCard | 밴률 상위 챔피언 + 클라이언트 정렬 | 1 |

**누적 코드 리뷰 패치:** 7개
**TypeScript 오류:** 0건 (전 스토리)
**Chart.js 번들 침투:** 0건 (NFR5 완전 준수)

---

## 잘 된 것들 (Successes)

1. **shadcn CSS 변수 매핑 조기 확립 (2.1)** — 이후 스토리에서 `bg-muted`, `text-muted-foreground` 즉시 동작. 기반 스토리 투자 효과 확인.

2. **React Query 캐시 공유 전략** — `useChampions()` queryKey 재사용으로 2.3, 2.4가 단 한 번의 API 호출 공유. NFR3(LCP 3초) 기여.

3. **코드 리뷰 학습 전이** — 2.2 패치(Skeleton 구조, `key={id}`)가 2.3 스펙에 사전 반영 → 2.4는 해당 패치 없이 통과. 스프린트 내 학습 사이클 검증.

4. **import type 100% 준수** — `verbatimModuleSyntax: true` 환경에서 전 스토리 TypeScript 0 오류.

---

## 개선이 필요한 것들 (Challenges)

### 반복 이슈: Skeleton 구조 불일치 (2.2 P3, 2.3 P2)

- **원인:** 스펙에 "Table 구조 일치" 지시만 있고, 실제 JSX 코드 샘플 없음
- **영향:** 코드 리뷰에서 2회 연속 패치 필요

### CSS 변수 미정의 참조 (2.1 P1)

- **원인:** 스펙 작성 시 `--color-bg-elevated` 존재 여부 미확인
- **영향:** 코드 리뷰에서 bad_spec으로 분류, 즉시 수정

### 서버 응답 순서 의존 (2.4 P1)

- **원인:** AC "내림차순" 명시에도 클라이언트 정렬 미구현
- **영향:** Acceptance Auditor가 AC1 위반으로 탐지

---

## 액션 아이템 (Epic 3에 적용)

| ID | 액션 | 우선순위 |
|----|------|---------|
| A1 | 스펙 Skeleton JSX 구조를 Dev Notes에 코드 블록으로 명시 | High |
| A2 | 신규 CSS 변수 참조 전 `global.css` `:root`에서 존재 확인 | High |
| A3 | 정렬 기준이 AC에 명시된 경우 클라이언트 정렬을 스펙 체크리스트에 추가 | Medium |

---

## 정착된 패턴 (유지)

- `key={entry.id}` (숫자 ID 사용)
- `import type` 필수
- `|| undefined` (falsy 차단, `?? undefined` 대신)
- `useMemo` 데이터 그룹핑
- React Query queryKey 공유 캐시 전략
- DragonContext `champions.get(championId)` 패턴

---

## Epic 3 시작 전 준비 체크리스트

- [ ] Chart.js `React.lazy` 동적 로딩 패턴 — 스토리 3.2 스펙에 명시 (NFR5 방어)
- [ ] `/champions/:id`, `/players/:riotId`, `/matches/:id` 라우트 — Story 3.1 선행 추가
- [ ] `usePlayers()`, `useMatches()` 훅 — 기존 구현 타입 확인 후 스펙 작성
- [ ] Breadcrumb 컴포넌트 — Story 3.7 마지막 배치 확인
